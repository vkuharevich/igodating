enum QuestionAnswerType {
    FREE_FORM,
    CHOICE,
    MULTIPLE_CHOICE,
    NUMERIC
}

enum RuleAccessType {
    PUBLIC,
    PRIVATE
}

enum RuleMatchingType {
    EQUALS,
    NOT_EQUALS,
    LIKE,
    IN_RANGE,
    MORE_THEN,
    LESS_THEN,
    IN_SET,
    SEMANTIC_RANGING
}

enum UserQuestionaryStatus {
    DRAFT,
    ON_PROCESSING,
    PUBLISHED,
    NEEDS_CHANGES,
    DELETED
}

type Query {
    answerTypes: [QuestionAnswerType]
}

type Mutation {
}
